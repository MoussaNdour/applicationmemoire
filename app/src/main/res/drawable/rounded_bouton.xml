<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Couleur de fond -->
    <solid android:color="#3581E3" />

    <!-- Coins arrondis -->
    <corners android:radius="20dp" />

    <!-- Bordure (facultatif) -->
    <stroke
        android:width="2dp"
        android:color="#3581E3" />

    <!-- Marges intérieures (facultatif) -->
    <padding
        android:left="10dp"
        android:top="5dp"
        android:right="10dp"
        android:bottom="5dp" />
</shape>
